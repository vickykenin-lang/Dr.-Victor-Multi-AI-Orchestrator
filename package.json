{
  "name": "victor-telegram-webhook",
  "version": "0.1.0",
  "private": true,
  "description": "Pinned Cloudflare Worker deployment toolchain for Dr. Victor.",
  "scripts": {
    "deploy": "wrangler deploy --var VICTOR_DEPLOY_GIT_SHA:$WORKERS_CI_COMMIT_SHA --var VICTOR_BUILD_UUID:$WORKERS_CI_BUILD_UUID",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  }
}
