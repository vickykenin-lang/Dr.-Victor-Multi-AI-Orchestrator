# Vision — Production House Process Only

After Topic OK the job runs unattended. Do not wait for a chat message between stages.

Owner gate: Topic OK only.
Look OK and Master OK are Director gates, not owner gates, unless Director failed twice.

Auto: every stage PASS waits gaps.json then the next stage starts.
A 15-minute scanner reruns any job that is not done, so a missed self-trigger cannot stall the house.

Identity still freezes at look_lock. Face drift = fail picture and return to keyframes. No manual frame edits.
